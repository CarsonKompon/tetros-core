@using System;
@using Sandbox.UI;
@attribute [StyleSheet]

@inherits Sandbox.UI.NavHostPanel
@namespace Tetros

<root>

</root>

@code
{
    public Action<int[]> UpdateBoard = delegate { };
    public Action<BlockType, Vector2, int> UpdatePlayer = delegate { };
    public Action<BlockType> UpdateHeldPiece = delegate { };
    public Action<BlockType[]> UpdateNextPieces = delegate { };
    public Action<long> UpdateScore = delegate { };
    public Action StartGame = delegate { };
    public Action EndGame = delegate { };

    public Action<string> ServerUpdateBoard = delegate { };
    public Action<BlockType, int, int, int> ServerUpdatePlayer = delegate { };
    public Action<BlockType> ServerRequestHeldPiece = delegate { };
    public Action<string> ServerRequestNextPieces = delegate { };

    public Action<long> OnExit = delegate { };

    public TetrosMenu()
    {
        DefaultUrl = "/";

        AddDestination("/", typeof(TetrosGamePage));
    }
}